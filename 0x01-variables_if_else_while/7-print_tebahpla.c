#include<stdio.h>

/*
 * main
 *
 * Return: Always 0 (Success)
 */

int main()
{
	 int i;
        // loop through the lowercase alphabet in reverse order
for (i = 'z'; i >= 'a'; i--) {
    putchar(i);
}

return 0;

}
